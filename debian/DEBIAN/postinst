#!/bin/bash
set -e

# Register the met-daemon unit (gated by its ConditionPathExists marker, so
# enabling it is inert until the use_rust_serial experiment is turned on).
systemctl daemon-reload 2>/dev/null || true
systemctl enable met-daemon.service 2>/dev/null || true
